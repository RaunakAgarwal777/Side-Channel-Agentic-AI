# Frontend — Operator Console

Local dev:
```bash
npm install
npm run dev
```
Opens at http://localhost:5173. Runs in mock mode by default (simulated agent trace) —
set `VITE_MOCK_MODE=false` and point `VITE_API_URL` at your backend once `/analyze` is live.
